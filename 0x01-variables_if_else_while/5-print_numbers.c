#include <stdio.h>

/**
 * main - Entry point
 * program to print numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf(i, "\n");
	}
	return (0);
}
