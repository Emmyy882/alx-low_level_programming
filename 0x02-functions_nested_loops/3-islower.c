#include "main.h"
#include <stdlib.h>

/**
 * _islower - checks for a lowercase character and returns 1 if true else 0
 * c - is a parameter
 */
int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
